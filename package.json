{
  "name": "usecase-pattern",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc --build",
    "brun": "npx tsc --build && node build/src/index.js"
  },
  "author": "jarzt",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.4.2"
  }
}
